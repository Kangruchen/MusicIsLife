# 激光铺面层指南

目标：节奏点继续在 ArrowVortex 里编辑和预览，Godot 只根据额外的 NOTES layer 解释激光规则。

## Layer 命名

在同一个 `.sm` 文件里保留原来的主谱面，再新增两个可选 NOTES layer：

- `LaserEcho`：先按谱面节奏闪预警，下一小节按同样位置和节奏发射。
- `LaserChord`：同一行的多个位置一起亮，下一小节按位置号发射。

解析器会在 NOTES 的 metadata 里查找 `LaserEcho` / `LaserChord`，大小写、空格、下划线和短横线都不敏感。推荐把 NOTES 的 description 写成 `LaserEcho` 或 `LaserChord`。

## 位置含义

激光 layer 的 4 列不再表示原来的 LDUR 动作，而是表示激光位置：

```text
第1列 = 1号位
第2列 = 2号位
第3列 = 3号位
第4列 = 4号位
```

普通主谱面仍然保持现有映射：左/下为 HIT，上为 GUARD，右为 DODGE。

## LaserEcho

示例：2 号位按四分拍闪 4 次，下一小节按同样节奏发射。

```text
0100
0000
0100
0000
0100
0000
0100
0000
,
```

如果要半拍、三连音或更密的节奏，直接在 ArrowVortex 里切换网格。解析器按小节行数计算拍点，不需要手写 beat 数字。

## LaserChord

示例：1、3、4 号位同时预警，下一小节第 1、3、4 拍分别发射。

```text
1011
0000
0000
0000
,
```

位置号会映射到下一小节内的拍号：

```text
1号位 -> 第1拍
2号位 -> 第2拍
3号位 -> 第3拍
4号位 -> 第4拍
```

## 场景配置

旧谱的普通 GUARD 激光仍使用 `guard_position_nodes`，不会因为新功能改变轮换位置。

新激光 layer 使用 `laser_pattern_position_nodes` 和 `laser_pattern_warn_position_nodes`。主场景和教程场景已经配置了 4 个 Laser 节点；以后如果换布局，只需要调整这两个数组。

## 示例文件

示例谱面在：

```text
assets/charts/laser_pattern_layers_example.sm
```
